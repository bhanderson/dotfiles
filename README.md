dotfiles
========

trying again

to install:

     ln -s ~/code/dotfiles/vimrc ~/.vimrc
     ln -s ~/code/dotfiles/.vim ~/.vim
     cd ~/code/dotfiles
     git submodule init
     git submodule update
